<h1>Localhost:3000/pictures</h1>
<h1>Pictures (<%= @pictures.length %>)</h1>

<%= link_to "Add Pictures", pictures_new_url %>

<% @pictures.each do |picture| %>
	<p><%= picture.title %></p>
	<p><%= picture.artist %></p>
	<%= link_to image_tag(picture.url), "/pictures/#{picture.id}" %>
<% end %>